# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Canvas {
  canvasData: String
  id: ID!
  name: String!
  ownerId: String!
}

type Mutation {
  createCanvas(name: String!, ownerId: String!, ownerName: String!): Canvas!
  removeCanvas(id: ID!): Canvas
  updateCanvas(updateCanvasInput: UpdateCanvasInput!): Canvas!
}

type Query {
  canvas(id: ID!): Canvas
  canvases(ownerId: String!): [Canvas!]!
  sayHello: String!
  user(id: ID!): User
  users(limit: Int, offset: Int, search: Int): UsersResponse!
}

input UpdateCanvasInput {
  canvasData: String
  id: ID!
  name: String
}

type User {
  email: String!
  id: ID!
}

type UsersResponse {
  hasMore: Boolean!
  limit: Int
  offset: Int
  total: Int!
  users: [User!]!
}